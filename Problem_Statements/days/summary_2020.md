# AoC 2020 Summary

> Rows 00-13 were solved during this repo's Prolog era and their notes
> describe that implementation; the maintained solutions are now the
> Python ones in `python/`, which produce the same answers. Day 14 onward
> is Python-first. See the [README](../../README.md).
> 
> **Part 1 / Part 2 columns hold submitted, accepted answers.** A day whose
> answers have not been submitted shows `?` and carries `LOCKED = None` in
> its test module.

| Day | Status | Part 1 | Part 2 | Notes |
|---:|---|---:|---:|---|
| 00 | ✅ | 3481005 | 5218616 | Tutorial dry run (Rocket Equation); `maplist`/`sum_list`, recursive `total_fuel/2`. 7/7 tests pass. |
| 01 | ✅ | 902451 | 85555470 | Report Repair; k-SUM via nondeterministic `k_sum/4` (take/skip DFS with `X =< Target` pruning on `msort`ed input), `once/1` + `foldl` product. 8/8 tests pass. |
| 02 | ✅ | 460 | 251 | Password Philosophy; first DCG parse (`entry//1` via `dcg/basics`) into `entry/4` records, policies as predicates counted with `include/3`, XOR via if-then-else. 11/11 tests pass. |
| 03 | ✅ | 148 | 727923200 | Toboggan Trajectory; first 2-D grid (list of char-lists), strided accumulator walk `slope_trees/4` with `mod`-wrapped columns and `drop/3` row skipping, five-slope product via `maplist`+`foldl`. 9/9 tests pass. |
| 04 | ✅ | 250 | 158 | Passport Processing; blank-line blocks → `Key-Value` pair lists, `forall/2` required-fields check, `valid_field/1` clause-per-rule validation table (`once(phrase(...))` for heights). 10 tests + 11 forall sub-tests pass. |
| 05 | ✅ | 888 | 522 | Binary Boarding; F/L=0, B/R=1 so each pass is a 10-bit number equal to its seat ID — `foldl` Horner-decode over a 4-fact `bit/2` table, part 1 = `max_list/2`, part 2 = sorted `gap/2` scan for the width-2 hole. 7 tests + 4 forall sub-tests pass. |
| 06 | ✅ | 6683 | 3122 | Custom Customs; each person an `ordset` of letters, groups via Day 4's reused `blocks/2` — part 1 sums `ord_union/2` sizes ("anyone" = ∪), part 2 sums `ord_intersection/2` sizes ("everyone" = ∩), each `maplist`+`sum_list`. 9 tests pass. |
| 07 | ✅ | 370 | 29547 | Handy Haversacks; rules → weighted digraph as an `assoc` (colour → `Count-Colour` edges). Part 1 reverses the edges and BFS-closes shiny gold's ancestors with a visited set (O(V+E); naive forward `can_contain/3` was ~exponential, 47ms→3.9ms); part 2 is the recursive weighted count `Σ nᵢ·(1+inside(cᵢ))`. 10 tests pass. |
| 08 | ✅ | 1331 | 1121 | Handheld Halting; first interpreter — program is an `assoc` PC→`Op-Arg`, run is a fetch/decode/execute loop (`exec/6` first-arg indexed on opcode) with a "seen" PC set; `run/3` reports `loop` vs `halt`. Part 1 reads the accumulator at the first repeat; part 2 brute-forces each `jmp`↔`nop` flip (pure O(log N) `put_assoc` patch) until one halts. 7 tests pass. |
| 09 | ✅ | 1930745883 | 268878261 | Encoding Error; integer list with a sliding preamble window, explicit distinct-value pair check, and positive-number contiguous-range scan for the encryption weakness. 7 tests pass. |
| 10 | ✅ | 2482 | 96717311574016 | Adapter Array; `msort` + outlet/device sentinels make one canonical chain, so part 1 is an adjacent-difference histogram (73 ones × 34 threes). Part 2 is DAG path counting — ascending order *is* a topological order, so a single `foldl` carries a 3-pair `Value-Ways` frontier and applies `ways(v)=Σ ways(v−1..v−3)` (tribonacci); ~97 trillion arrangements, free via bignums. `differences/2` rewritten with an accumulator so indexing splits `[]`/`[_|_]` and the sweep is choicepoint-free. 11 tests pass. |
| 11 | ✅ | 2321 | 2102 | Seating System; first cellular automaton. Both parts are one simulation differing only in neighbour relation and tolerance (4 vs 5), so the grid is thrown away up front: floor dropped, 7423 seats numbered 1..N, and each seat's neighbours precomputed into a compound term `nbrs(...)` read with O(1) `arg/3` (a list + `nth1` would be quadratic per round; an assoc a 13× tax on 4.5M reads). State is `occ(0/1,...)`, rebuilt fresh each round — immutability *is* the "simultaneous update" rule. Part 2's rays use a committed-choice `first_seat/7` (blocking is by seat, not occupancy). Fixpoint at 86/87 rounds; termination is the puzzle's promise, not a proof — hysteresis breaks the Goles–Olivos antitone argument. 13 tests pass; 0.40s + 0.45s. |
| 12 | ✅ | 1956 | 126797 | Rain Risk; the Part Two twist is a *reparameterisation*, not a rewrite — carry `nav(X,Y,VX,VY)` (ship + one vector) and `F` moves the ship along the vector, `L`/`R` rotate the vector, in both parts. Part 1's "facing" is just a unit vector starting east, so the entire difference is whether `N/S/E/W` push the ship or the vector: one predicate, `translate/5`, indexed on `Mode`. Rotation is exact integer work — one clockwise quarter turn is `(x,y)→(y,−x)`, i.e. multiplication by `−i` on a Gaussian integer, the cyclic group C₄ enumerated as four `rotate/5` facts (no trig, no drift). `L` folds onto `R` via `-(D//90) mod 4`, which is right *only* because SWI's `mod` is floored — C/Rust `%` would hand back `-1` here. `foldl/4` over 769 `Action-Value` pairs, O(n), and parsing outcosts either part. 17 tests + 5 forall sub-tests pass; 0.6ms total. |
| 13 | ✅ | 102 | 327300950120029 | Shuttle Search; bus `N` departs whenever `T mod N =:= 0`, so part 1 is one floored `mod` — the wait is `(-Earliest) mod Id`, minimised via a `Wait-Id` pair under the standard order of terms (C/Rust `%` would hand back a negative here, day12's trap again). Part 2 is not searchable: the answer is 3.27e14, so it is restated as nine simultaneous congruences `t ≡ -Offset (mod Id)` and solved by the **Chinese Remainder Theorem**. The IDs are distinct primes, so a unique solution exists mod their product (1215475766514841). Implemented as an incremental sieve rather than modular inverses: `foldl` a `T-Step` pair meaning "solutions so far are T, T+Step, …", striding the current progression until the next bus fits, then `Step := Step*Id`. Coprimality bounds each scan by `Id`, so the cost is ΣId (≤1249) not ΠId — **911 strides** total, 2,941 inferences. Offsets are list positions, so `x` entries are dropped but still counted (`nth0/3` with a free index as generator inside `findall/3`). 19 tests + 14 forall sub-tests pass; 0.73ms total. |
| 14 | ✅ | 3059488894985 | 2900994392308 | Docking Data; a 36-bit emulator, and the first day written Python-first. Part Two is a *reinterpretation* of the same instruction stream rather than a new algorithm (day 12's trick): `0` and `X` swap meaning between the parts, and `X` goes from "leave alone" to "write both ways". Part 1 is two precomputed integers per mask — `value & zeros | ones`, where an `X` position is 0 in `ones` and 1 in `zeros` and so is the identity for both ops. Part 2 enumerates floating addresses by **counting**: bit *j* of a 0..2^k-1 counter selects the *j*-th `X` position, scattering k contiguous bits out to k arbitrary ones — every subset once, no recursion. Memory is a dict, so the 2^36 address space costs nothing and "initialized to 0" is free. Real input: 460 stores under 100 masks carrying 4-9 `X` bits → 75,328 stores on 71,385 distinct addresses. 20 tests pass; 0.28ms parse / 0.07ms part1 / 67.5ms part2. |
| 15 | ✅ | 1665 | 16439 | Rambunctious Recitation; the Elves' memory game is **Van Eck's sequence** (OEIS A181391) — no closed form, no shortcut, so both parts are one engine `play(starting, final_turn)` walked turn by turn, stopped at 2,020 vs 30,000,000. State is a single `last_seen` dict + the current number held *outside* it: the table deliberately lags one occurrence behind, so "how many turns apart" is one lookup, then the table advances — O(1)/turn, O(n) total. At 30M turns: 3.61M distinct numbers spoken (~12% of turns still say something new), largest 29,419,548 < n, which licenses a flat-array sidebar (2.4s vs dict 3.6s; same loop in Rust ~0.3s — it's a memory-latency benchmark). Slowest day yet at ~6× day 11. 23 tests pass. |
| 16 | ✅ | 21996 | 650080463519 | Ticket Translation; two filters in series. Part 1 is value-level — a value no field's inclusive spans accept is corrupt — summed over 238 nearby tickets (48 bad values on 48 tickets, every one below 25 or above 974: the twenty rules' spans union to the gap-free window 25..974). Part 2 drops those tickets and builds one candidate set per column (fields whose spans accept every value in it), then **peels singletons**: a column with one candidate is forced, strike that field elsewhere, repeat. Sound because a forced column holds that field in every matching; complete because the real input's 20 sets form a full staircase (sizes exactly 1..20, nested — pinned as a test, not prose), so each round forces one column. Raises rather than guesses when no column is forced. Answer = product of my ticket's six `departure` columns. 25 tests pass; 0.42ms parse / 4.3ms part1 / 32ms part2 (a 1000-entry field-mask lookup table, in the guide sidebar, is 21× faster on part 2). |
| 17 | ✅ | 359 | 2228 | Conway Cubes; Life lifted out of the plane -- the same rule (active survives on exactly 2 or 3 neighbours, inactive is born on exactly 3) in 3-D (26 neighbours) for part 1 and 4-D (80) for part 2, six cycles each. One engine, `boot(slice, dims)`: the input slice is padded with zeros onto the extra axes and the state is a **set of active coordinate tuples**, so the infinite grid is free and there is no array to resize. A cycle is a **scatter**, not a gather: every active cell adds 1 to a `Counter` at each of its 3^d-1 neighbours, and the rule is one filter over the counter (cells nobody touched have 0 neighbours and correctly never appear). Real input 8x8 with 31 active: grows 31→359 in 3-D and 31→2228 in 4-D (peaking at 2556 after cycle 5), inside a 20x20x13x13 box. The state stays mirror-symmetric in z and w and under z↔w (pinned on the example and real input), which the guide's folded-simulation sidebar exploits; the 2-D case is plain Life and the example is a glider (also a test). 31 tests pass; 0.004ms parse / 12.5ms part1 / 229ms part2 -- 383k tuple builds in 4-D; unpacking fixed-width tuples is 3.9× faster, Rust HashSet port 7.6ms. **Alt:** `day17_alt.py` is the flat brute-force scan (fixed 30×30×15×16 box, one `for` per axis, 80 set lookups per cell) kept as the read-it-first version; same answers, 0.18 s / 10.1 s, 18 agreement-and-box-edge tests (two marked `slow`), guide §8. |
| 18 | ✅ | 650217205854 | 20394514442037 | Operation Order; arithmetic homework under changed precedence -- part 1 gives `+` and `*` equal rank (strict left to right), part 2 makes `+` outrank `*`. One evaluator, the classic **two-stack machine** (shunting-yard evaluating as it goes): a `values` stack and an `ops` stack; a number is pushed, an operator first reduces every waiting operator whose precedence is `>=` its own (the `>=` is what makes equal rank associate left -- pinned as a test) then waits, `(` is a fence, `)` reduces to the fence. The precedence table is the only difference between the parts: `{+:1, *:1}` vs `{+:2, *:1}`. Invariant: within a fence segment the operator stack is strictly increasing in precedence, so part 1 is a running total (pinned as a left-fold test) and part 2 is the product of the sums between `*`s (pinned). Real input 370 lines / 10,528 tokens, single digits 2-9, nesting <= 2; stacks peak at exactly the predicted 5/4 (part 1) and 8/7 (part 2). Tests also evaluate all 370 lines through an independent oracle -- Python's own `eval` with the operator symbols relabelled onto an `int` subclass -- and both tables agree line by line; a third, school-precedence table `{+:1, *:2}` agrees with plain `eval` on every line too, showing the evaluator is a general precedence machine. All six statement examples pass under both tables. 42 tests pass; 1.6ms parse / 1.4ms part1 / 1.5ms part2 (Rust port 0.25 / 0.09 / 0.09 ms; recursive descent, regex-collapse and the eval trick timed in the guide sidebar). |
| 19 | ✅ | 111 | 343 | Monster Messages; a numbered grammar (literal, or alternatives of rule-id sequences) and messages to accept in full. One recursive function, `match_ends(rules, id, text, start)`, returns the **set of every end position** a rule can stop at -- a literal gives `{start+1}` or nothing, a sequence threads the frontier of positions through its sub-rules, an alternation unions -- and a message matches when `len(text)` is in rule 0's set from 0 (the statement's `aaaabbb` ends in `{6}` with length 7: the "extra unmatched character" is a fact in the set). Part 2's loops `8: 42 \| 42 8` and `11: 42 31 \| 42 11 31` are two lines of **data** (`PART2_LOOPS`, pinned as `parse_rule` of the statement text); the same function runs them because every rule consumes a character and the self-reference is never leftmost, so `start` strictly grows along any recursive chain (max depth 11 / 17 on the real input; a 100-chunk message is a test). Real input 132 rules / 427 messages, all lengths multiples of 8: rules 42 and 31 each match exactly 128 eight-character strings and **partition** all 256, so every message is a word over {A, B}; part 1 (`42 42 31`) is `AAB`, part 2 is `AᵐBⁿ` with m > n >= 1. Tests pin the partition and count both parts that way as an independent oracle; the general matcher agrees (111 / 343, both submitted and accepted, `LOCKED` asserts them). Statement's part 2 example: 3 then 12, exact message sets checked. 36 tests pass; 0.15ms parse / 22ms part1 / 190ms part2 -- 667k `match_ends` calls in part 2 (Rust port 5.2 / 41 ms, only 4-5× because the redundancy is the algorithm's); memoised 70ms, regex-compiled grammar 2.2ms, chunk tagging 0.6ms, all in the guide sidebar. |
| 20 | ✅ | 27803643063307 | 1644 | Jurassic Jigsaw; 144 shuffled 10×10 tiles, each rotated/mirrored at random, that fit into a 12×12 image because neighbours share a border exactly. Everything is a **tuple of strings** (tile, image, sea monster alike), and two functions, `rotate` (quarter turn clockwise) and `flip`, generate the eight orientations of any block -- the D4 group facts are tests. Edges are read in one fixed direction (top/bottom left→right, left/right top→bottom) so fitting is plain string equality, no reversing. Assembly is a **depth-first search** filling the grid row-major: every tile becomes eight `Piece`s with its four edges read out, indexed by top edge and by left edge; a cell's candidates are the pieces whose top matches the bottom above (first row: whose left matches the right neighbour), skipping used IDs, backing up on no fit. Complete and assumption-free, but the real input's borders are **unique** (312 distinct, 264 seams = 2·12·11, 48 perimeter = 4·12, none palindromic; tiles split 100/40/4 by unmatched edges -- pinned) so each constrained cell has one real candidate and the search is ~1 ms: 139 starts, 1,701 placements. Part 1 multiplies the assembled grid's corner IDs; the classic edge-count shortcut is the test oracle (same four tiles) and a 20× faster sidebar. Part 2 stitches the 8×8 interiors into 96×96 (2,199 `#`), scans all eight orientations for the 20×3 fifteen-cell monster (offsets derived from the picture, not typed), takes the orientation covering the most cells as a set (overlaps once, never negative -- a 3×40 solid sea is the test), and subtracts: exactly one orientation has monsters, 37 of them, 555 cells, no overlap. Sample: statement grid reproduced up to orientation, stitched image equals the statement's 24×24 (and that equals the arrangement with borders stripped), 303 `#` − 2×15 = 273. 27803643063307 / 1644, both submitted and accepted, `LOCKED` asserts them. 36 tests pass; 0.21ms parse / 10.6ms part1 / 40.3ms part2 -- 9 ms of each part is building 1,152 oriented pieces and 26 ms of part 2 is the 57,904-anchor monster scan (regex rows 4.8 ms; Rust port 0.08 / 1.2 / 1.6 ms, edges as 10-bit ints). |
| 21 | ✅ | 2072 | fdsfpg,jmvxx,lkv,cbzcgvc,kfgln,pqqks,pqrvc,lclnj | Allergen Assessment; 38 foods, each an unreadable ingredients list tagged with some of its allergens. Two rules do all the work: an allergen lives in exactly one ingredient, and a food that lists it must contain that ingredient -- so an allergen's **candidates are the intersection** of the ingredient sets of every food naming it (a food that names nothing is no evidence, pinned as a test: unlabelled foods must not strike anything). Part 1 counts appearances of ingredients in no candidate set (2,338 appearances, 266 suspect, 2,072 inert on the real input) and needs no resolution at all -- a test has two indistinguishable allergens where part 2 must raise and part 1 still answers. Part 2 is day 16's **singleton peeling** on a dict: a one-candidate allergen is forced, strike its ingredient elsewhere, repeat; refuses to guess. Not a staircase this time (starting sizes 1,2,2,2,3,3,4,4; six rounds, one of them forcing three at once) but every round forces something. The identity linking the parts is proven and pinned on the real input: when peeling completes, the union of the candidate sets *is* the set of holders, so no suspect is inert. Answer is the year's only string: holders sorted by allergen, comma-joined (sample `mxmxvkd,sqjhc,fvjkl`). 2072 / `fdsfpg,jmvxx,lkv,cbzcgvc,kfgln,pqqks,pqrvc,lclnj`, both submitted and accepted, `LOCKED` asserts them. 0.077ms parse / 0.140ms part1 / 0.055ms part2, eighth fastest day (Rust port with `[u64; 4]` ingredient bitsets: 0.07 / 0.0025 / 0.0021 ms -- parts 50× faster, parse no faster since interning 2,338 names costs what `str.split` never pays; a Python bitmask variant is 12× faster on part 1 and slower overall for the same reason, guide §7). **Prolog companion** `prolog/day21.pl` (first of its kind, outside the frozen tree): part 2 is a three-clause backtracking `assign/3` -- `member` is rule 1, `\+ memberchk` is rule 2 -- 63 inferences to the first solution, and `aggregate_all(count, ...)` answers "is it unique" (1) as a query where the Python needed a proof; 3 pytest cases run it through `swipl` and skip without one. 23 tests pass. |
| 22 | ✅ | 32413 | 31596 | Crab Combat; two 25-card decks of the numbers 1..50 and a game of War: higher card takes both, winner's card goes under first, play until one player holds all fifty, score the deck bottom-up (weights 1..n). Part 2 is **Recursive Combat**, the same game with two rules added -- a repeated position (both decks, in order) in the same game ends it for player 1, and a round where both players hold at least as many cards as the value just drawn is settled by a sub-game on *copies* of that many cards, so the winner may collect with the lower card. One engine `combat(decks, recursive)` plays both parts: decks are **tuples** (draw is `d[0], d[1:]`, collect is `d + (a, b)`, a sub-game's decks are slices, and a tuple is its own hashable snapshot for the per-game seen-set, keyed on *both* decks as the statement says -- the common deck-1-only key is 4× faster and wrong in principle). Regular Combat keeps the seen-set too and raises on a repeat instead of spinning (the statement's 43,19 / 2,29,14 loop example cycles in six rounds under either rule set). Sample: 29 rounds / 306, then five games three deep / 291, both final decks reproduced exactly; the sample plays one sub-game (8 vs 10,9,7,5) twice, which pins "previous rounds from other games are not considered". Real input: part 1 is 265 rounds; part 2 is 12,487 games, 986,517 rounds, ten deep, and **3,523 games end by the loop rule** -- without it the top game does not finish in 5M rounds. An identity is proven and pinned, not shipped: the highest card in a game of n distinct cards is >= n, so it can never trigger a sub-game and is never lost; player 1 holding it in a sub-game is a win (9,440 of 12,486 sub-games, all won), player 2 holding it is not decisive (3,046: 2,434 to player 2, 612 to player 1 by the loop rule). 32413 / 31596, both submitted and accepted, `LOCKED` asserts them. 27 tests pass; 0.008ms parse / 0.140ms part1 / 420ms part2, third slowest day (Rust port 0.0006 / 0.040 / 138 ms -- only 3×, the per-round copies and hashing are the algorithm's; high-card shortcut 2.7 ms in Python, 0.9 ms in Rust; deque 500 ms, memoised sub-games 428 ms, guide §7). |
